<%- include('../../partials/header') %>
<body>

    <%- include('../../partials/navbar') %>

    <div class="container">
        <br>
        <div class="card">
            <div class="card-header">
                <h2>Editar de categoria</h2>
            </div>
            <div class="card-body">
                <form action="/categories/update" method="POST">
                    <input type="hidden" name="id" value="<%= category.id %>">
                    <div class="form-group">
                        <label>Título</label>
                        <input type="text" name="title" class="form-control" value="<%= category.title %>" placeholder="Defina o título da categoria">
                    </div>
            </div>
            <div class="card-footer">
                    <button class="btn btn-sm btn-warning">Atualizar</button>
                    <a class="btn btn-sm btn-secondary" href="/admin/categories">Voltar</a>
                </form>
            </div>
        </div>
    </div>
        
</body>
<%- include('../../partials/footer') %>